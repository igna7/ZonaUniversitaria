- if user_signed_in?
	%p#intro
		Hola
		= current_user.name
		%br/
		%span
			Comparte tu inspiración con nuestra comunidad
- else
	%p#intro
		¿Que es lo que te inspira?
		%br/
		%span
			Comparte tu inspiración con nuestra comunidad

#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:small)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Escrito por
						= post.user.name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size